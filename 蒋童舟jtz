#include <bits/stdc++.h>
#include <windows.h>
#include <tchar.h>
#include <assert.h>
#include <shellapi.h>
using namespace std;
int main()
{
  while(1)
  {
    cout<<"蒋童舟jtz"<<endl;
    cout<<"联系方式请输1，小店请输2，意见反馈请输3，内部控件请输4"<<endl;
    int a;
    cin>>a;
    if (a==1)
    {
      ShellExecute(NULL,_T("open"),_T("explorer.exe"),_T("https://mp.weixin.qq.com/s/-S1enikYe8Mz0Q0o5X9ObQ"), NULL, SW_SHOW);
    }
    else if (a==2)
    {

      cout<<"此处有bug，您可以将小店链接复制到浏览器打开  ";
      cout<<"链接：https://m.hfhomes.cn/html/home/#/cloudShop/wMallShop?shareId=332154&shopId=40994&shareBUserId=332154" <<endl;
      ShellExecute(NULL,_T("open"),_T("explorer.exe"),_T("https://m.hfhomes.cn/html/home/#/cloudShop/wMallShop?shareId=332154&shopId=40994&shareBUserId=332154"), NULL, SW_SHOW);
    }
    else if (a==3)
    {
      cout<<"意见反馈发送至jiangtongzhou@jiangtongzhou.com"<<endl;
    }
    else if (a==4)
    {
      cout<<"请输入密码：";
      int b;
      cin>>b;
      if (b==6198) cout<<"无"<<endl;
      else cout<<"密码错误"<<endl;
    }
    else
    {
      cout<<"不要皮，请输入正确的指令"<<endl;
      break;
    }
    cout<<"如要退出该程序请输5，继续运行请按6"<<endl;
    int b;
    cin>>b;
    if(b==5)
    {
      cout<<"请按enter键以结束。"<<endl;
      return 0;
    }
    if(b==6)
    {
      system("cls");
    }
    else
    {
      cout<<"不要皮，请输入正确的指令"<<endl;
      break;
    }
  }
  return 0;
}
